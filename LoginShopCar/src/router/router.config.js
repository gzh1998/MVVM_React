
import Login from '../components/login';
import Shopcar from '../components/shopcar';

export default {
    routes: [{
        path: '/login',
        component: Login
    }, {
        path: '/shopcar',
        component: Shopcar
    }]
}